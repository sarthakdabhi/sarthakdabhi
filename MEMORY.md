# Project Memory: Sarthak Dabhi Portfolio

## Project Decisions
- **Verification Script:** Added `npm run check` to `package.json` to combine linting and type-checking. [2026-05-17]
- **Agent Instructions:** Initialized `GEMINI.md` with project context and conventions. [2026-05-17]
- **Security:** Added `settings.local.json` to `.gitignore` to protect local agent configurations. [2026-05-17]

## Future Work
- Consider adding a `Makefile` for a more universal verification entrypoint.
- Set up CI/CD (e.g., GitHub Actions) to run `npm run check` on pull requests.
